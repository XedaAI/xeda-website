# Supabase (Vite exposes VITE_* to the client — these are the public/anon values,
# safe to ship in the browser). Copy this file to `.env` and adjust if needed.
#
# Edge function secrets (RESEND_API_KEY, MAILCHIMP_*, ELEVENLABS_API_KEY,
# GEMINI_API_KEY) are NOT set here — they live in the Supabase dashboard.
# See docs/SUPABASE.md.
VITE_SUPABASE_PROJECT_ID="onqkzwsgnrwrtmxyfbyv"
VITE_SUPABASE_URL="https://onqkzwsgnrwrtmxyfbyv.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-anon-key"
